#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long double a;
    cin >> a;
    cout << fixed << setprecision(6);
    cout << 100 / 1.609344 / a * 3.785411784;
}