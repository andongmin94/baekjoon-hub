#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin >> t;
    double u = static_cast<double>(t) / 2;
    cout << fixed << setprecision(0) << u * u;
}
