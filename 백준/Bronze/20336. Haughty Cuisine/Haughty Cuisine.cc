#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    while(n--)
    {
        int a;
        cin >> a;
        cout << a << '\n';
        while(a--)
        {
            string s;
            cin >> s;
            cout << s << '\n';
        }
    }
}
