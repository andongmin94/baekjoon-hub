#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    if (n != 0) cout << fixed << setprecision(2) << 1.00;
    else cout << "divide by zero";
}
