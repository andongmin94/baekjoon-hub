#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int x, d; cin >> x >> d;
    if (x * 2 > d) cout << "take it";
    else cout << "double it";
}
