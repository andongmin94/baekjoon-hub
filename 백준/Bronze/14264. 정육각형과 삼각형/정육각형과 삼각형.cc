#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long double l;
    cin >> l;

    cout << fixed << setprecision(14) << l * l * sqrt(3) / 4;
}
