#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int i;
    cin >> i;
    int j = sqrt(i);
    cout << "The largest square has side length " << j << ".";
}
